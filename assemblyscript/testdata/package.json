{
  "name": "hello-assemblyscript",
  "version": "0.0.1",
  "scripts": {
    "build": "asc abort.ts --debug -b none -o abort.wasm --disable bulk-memory && asc random.ts --debug -b none -o random.wasm --disable bulk-memory && asc trace.ts --debug -b none -o trace.wasm --disable bulk-memory"
  },
  "devDependencies": {
    "assemblyscript": "^0.20.6"
  }
}
